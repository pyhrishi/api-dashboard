// Phase-1 scope: renders the REAL /console home page behind the scoped Phase-1 shell —
// the first-time-user experience exactly as the original: FTUE "Welcome to zinbit" state
// (OnboardingChecklist + FirstCallWizard), the veteran command center after the first call,
// and the real "Reset FTUE State" button (resetPrototypeState).
export { default } from '@/app/console/page';
